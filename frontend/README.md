# FarmFlo Simulation Studio — Frontend

React application for the FarmFlo Simulation Studio dashboard.

## Development

```
npm install
npm run dev
```

The dev server runs on `http://localhost:5173` and proxies `/api` requests to the backend on `http://localhost:5000`.

## Build

```
npm run build
```
